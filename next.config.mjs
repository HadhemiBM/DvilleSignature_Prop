/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["tinypng.com", "images.pexels.com", "res.cloudinary.com"], // Add other domains as needed
  },
};

export default nextConfig;
